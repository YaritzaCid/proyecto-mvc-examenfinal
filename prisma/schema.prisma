generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Affiliate { 
  id             Int    @id @default(autoincrement())
  firstName      String
  lastName       String
  email          String @unique
  membershipType String
}